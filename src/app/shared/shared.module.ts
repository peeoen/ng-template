import { CommonModule } from '@angular/common';
import { NgModule } from '@angular/core';
import { FormsModule, ReactiveFormsModule } from '@angular/forms';

const EXPORT_DECRORATORS = [

];

const PIPES = [

];

const MODULES = [

];

@NgModule({
    declarations: [

    ],
    imports: [
        CommonModule,
        FormsModule,
        ReactiveFormsModule,
    ],
    exports: [

    ],
    entryComponents: [

    ],
    providers: [

    ]
})
export class SharedModule {}
